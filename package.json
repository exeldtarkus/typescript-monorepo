{
  "name": "typescript-monorepo",
  "version": "0.0.0",
  "description": "",
  "main": "build/src/index.js",
  "types": "build/src/index.d.ts",
  "files": [
    "build/src"
  ],
  "license": "Apache-2.0",
  "keywords": [],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint",
    "install:core-logger": "npx tsc --build lib/core-logger",
    "install:ms-user": "npx tsc --build services/ms-user",
    "build:core-logger": "npm run build --workspace=lib/core-logger"
  },
  "devDependencies": {
    "gts": "^6.0.2",
    "typescript": "^5.6.3",
    "@types/node": "^22.7.5"
  },
  "workspaces": [
    "lib/*",
    "services/*"
  ]
}
